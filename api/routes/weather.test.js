const handler = require('./weather');

describe('GET weather', () => {
  it('should return data with lat and lon', () => {
  });

  it('should return data with zip and locale', () => {
  });

  it('should return data with the right units', () => {
  });

  it('should fail if no params are provided', () => {
  });

  it('should fail if lat is provided and lon is not', () => {
  });

  it('should fail if lon is provided and lat is not', () => {
  });

  it('should fail if zip is provided and locale is not', () => {
  });

  it('should fail if locale is provided and zip is not', () => {
  });
});
